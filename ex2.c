#include <stdio.h>
int main()
{
    int arr[100], i, n;
    int low = 0, high, mid;
    printf("Enter nuumber of elements:");
    scanf("%d", &n);
    printf("Enter sorted elements:");
    for (i = 0; i < n; i++)
    {
        scanf("%d", &arr[i]);
    }
}